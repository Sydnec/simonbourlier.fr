version: '3.8'

services:
  events-api:
    build: .
    ports:
      - "3004:8084"
    volumes:
      - ./events:/root/events
      - ./events-config.json:/root/events-config.json
    environment:
      - ENV=production
      - PORT=8084
    restart: unless-stopped
